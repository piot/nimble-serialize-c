# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(nimble-serialize STATIC 
  client_out.c
  debug.c
  serialize.c
  server_out.c
  version.c)

include(Tornado.cmake)
set_tornado(nimble-serialize)

target_include_directories(nimble-serialize PUBLIC ../include)


target_link_libraries(nimble-serialize PUBLIC 
  flood)

